NEBIUS_API_KEY=
NEBIUS_AI_PROJECT=
TAVILY_API_KEY=

# Optional overrides. Both are comma-separated keyword filters resolved live
# against GET /v1/models (see greenlit/clients/nebius.py). Only set these if
# the default resolution fails or is ambiguous on your account.
# GREENLIT_PLAN_MODEL_KEYWORDS=nemotron,ultra
# GREENLIT_EVAL_MODEL_KEYWORDS=nemotron,nano
