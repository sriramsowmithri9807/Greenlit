# Demo fixtures

Placeholder. Populated in build-order step 6 with 2-3 fixture repos with
deliberately broken tests:

1. A simple one-shot fix.
2. A fix that needs 2-3 retries.
3. A fix that triggers the Tavily research branch (unfamiliar/version-sensitive
   external API).
